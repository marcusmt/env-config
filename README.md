## Dots for Ubuntu 24
```shell
cd $HOME
wget https://github.com/marcusmt/env-config/archive/refs/heads/ubuntu-i3.zip
unzip ubuntu-i3.zip
cd env-config-ubuntu-i3/
./install.sh configure && ./install.sh sysupdate
```