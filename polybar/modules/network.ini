[module/wlan]
type = internal/network
interface = wlp0s20f3

interval = 1

format-connected = <ramp-signal>
format-disconnected = <label-disconnected>

label-disconnected = 󱚵
label-disconnected-foreground = ${colors.danger}

ramp-signal-0 = 󰖩
ramp-signal-0-foreground = ${colors.icon}
